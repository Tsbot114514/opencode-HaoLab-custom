# Markdown 协作编辑器：PPT 内容

## 第 1 页：Word 式 Markdown 编辑器

### 标题

像编辑 Word 一样编辑 Markdown

### 核心内容

- Markdown 以 Word 页面形式渲染
- 用户直接阅读和修改排版后的正文
- 无需面对 Markdown 源码和格式符号
- 底层仍保存为开放、可读的 `.md` 文件

### 一句话介绍

用 Word 的方式编辑 Markdown，用 Markdown 的方式与 Agent 协作。

---

## 第 2 页：修订与批注

### 标题

像 Word 一样审阅修改

### 核心内容

- 新增文字显示为新增修订
- 删除文字保留删除线，不会直接消失
- 支持文字和图片批注
- 批注显示在正文右侧，并与原文位置对应
- 支持快速跳转到下一处修订或批注

### 示例

```markdown
{--原来的内容--}{++修改后的内容++}

{==需要讨论的文字==}{>>这里添加批注意见<<}
```

编辑器将这些标记渲染为 Word 式审阅效果，用户不需要直接操作标记语法。

---

## 第 3 页：人与 Agent 实时协作

### 标题

共同修改同一个 Markdown 文件

### 核心内容

- 用户在 Word 式页面中修改正文、修订和批注
- Agent 直接读取和修改同一个 Markdown 文件
- Agent 保存后，打开的文档自动更新
- 用户批注可以直接成为 Agent 下一轮修改依据
- 无需反复复制、粘贴、上传和下载文档

### 协作流程

```text
用户审阅并批注
        ↓
自动保存 Markdown
        ↓
Agent 读取批注并修改
        ↓
页面同步显示修改结果
        ↓
用户继续审阅
```

---

## 第 4 页：转换为 Word

### 标题

从协作 Markdown 到 Word 所见即所得文档

### 完整流程

```text
Markdown 内容
    ↓
Word 式渲染与编辑
    ↓
用户与 Agent 协作修订、批注
    ↓
确认最终版本
    ↓
转换为 Word 文档
```

### 最终效果

- Markdown 负责开放存储和 Agent 协作
- 编辑器负责 Word 式阅读、修订和批注
- Word 负责最终排版、交付和打印

### 总结

一个支持人机协作修订和批注的 Word 式 Markdown 渲染器，并可将最终内容转换为所见即所得的 Word 文档。
